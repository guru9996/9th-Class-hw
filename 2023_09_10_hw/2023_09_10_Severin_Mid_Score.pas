PROGRAM AverageScore(INPUT, OUTPUT);
CONST
  NumOfScores = 4;
VAR
  S1, S2, S3, S4, AveScr, AveRest, AveDown, AveUp, AveMath, IntAve: SHORTINT;
BEGIN
  WRITELN('??????? ?????? ??????:');
  READLN(S1, S2, S3, S4);

  IntAve := (S1 + S2 + S3 + S4) DIV NumOfScores;
  AveRest := (S1 + S2 + S3 + S4) MOD NumOfScores;

  AveDown := IntAve;
  AveUp := IntAve;
  AveMath := IntAve;
  IF AveRest > 0
  THEN
    BEGIN
      AveUp := AveUp + 1;
      IF AveRest >= 2
      THEN
        AveMath := AveMath + 1
    END;
  WRITELN('??????? ??????, ???????????');
  WRITELN('? ???????:', AveDown,' ? ???????:', AveUp,' ?????????????:', AveMath);
  READLN
END.
